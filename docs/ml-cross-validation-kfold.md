# Evaluation - K-Fold Cross Validation

Used on both evaluation and hyper parameter tunning, it repeatedly splits the dataset into training and validation sets. Instead of doing one _train/test split_, the dataset is split in equal parts(folds) and the model is trained K times using different train/validation splits
