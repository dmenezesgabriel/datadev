# Binning

Transform numerical features into categorical features by grouping values into ranges of values, or "bins".

- Quantile Binning: Divides the data into bins such that each bin has an approximately equal number of observations. This is useful for creating balanced categories.
